/** Public surface of the room3d feature. Everything else (engine internals,
 *  individual sub-components) is an implementation detail — import them
 *  directly from their files within this folder if truly needed, but prefer
 *  mounting `RoomScreen` as a whole. */
export { RoomScreen } from './RoomScreen'
